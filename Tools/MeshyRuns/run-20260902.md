# 런 로그 — 2026-09-02 (SPEC-SPELL-FIDELITY 1차 런)

정본 Spec: `Docs/Specs/SPEC-SPELL-FIDELITY.md`. 술식 정본 충실화 — 전수 감사(CSV 효과문 대 구현
괴리 5건) 문답 확정 후 구현. 방침 전이=DECISIONS #137(광역 실판정 개시)·#138(전용 구현 원칙).

## 구현 기록 (P0~P4·P6 — 코드·데이터)

- **판정**: `SpellBookSO.Entry`에 `AreaShape`(None/Cone)·`ProjectileSpeedMul` 신설 → `SpellCast`
  전달 → `CombatLoopWiring.ResolveConeAttack`(전방 수평 cone 40°/10m [TEST]·형성 딜레이 0.4s·
  `_enemies` 씬 배열 전수·대상 0체=허공 방사). 단일 경로는 `duration=dist/(18×mul)` — 탄속 분화가
  판정·연출 같은 시계(사 1.8/아 0.6/가 1.3 배선, SpellBook_Proto YAML 실측 확인)
- **노**: `FlameJetEffect` 신설(#138 1호 — 코드 구성 전용 PS: cone 25°·도달 8m·분사 1.0s·
  스트레치드 빌보드·색 백→틴트→먹빛·순간 Light 감쇠 소멸). SpellFx_No에 FlameJet 자식 추가
  (_flameMaterial=par_Fire 재사용 — 자원 허용면). 형성 0.3s=판정 딜레이 0.4s와 근사 동기(§8 예외 1)
- **가**: `ThornLanceEffect` 신설(세그먼트 6 순차 성장 랜스·스파인 대상 추적·EaseOutBack 팝·
  잔존 0.4s→침강). `SetImpactClock` 훅 신설(SpellSequenceEffect) — 어댑터가 판정 비행시간을
  밀어줘 랜스 도달=피해 착탄 동기. SpellFx_Ga 신설(Fly01-01 배리언트+Lance, 메시=CrystalSpike_So
  재사용·재질=M_SpellBodySpike)
- **패링**: 방어막 문양 수명=GuardDuration 4s(가드 프로파일 — 창 진함→방출 45% 감쇠→꼬리 1s
  스케일 스러짐, 글자 비례 스케일 보존) · 패링 5자 SpellVisualSet 등재(거=Fly06/너=Fly08/머=Fly04/
  서=Fly09/어=Fly05 — 광역 글자와 같은 속성 계열 원본) · Half=축소 버스트 0.5(보상 무변경)
- SpellVisualSet 15행 재직렬화(가→SpellFx_Ga 교체 포함)·C1_CombatLoop `_enemies[0]` 배선

## 생성 이력 (마 — 바위 실체, SPELL-FX 런 규율 준용)

| 어휘 | task ID | 설정 | 시도 | credits | 판정 | 경로 |
|---|---|---|---|---|---|---|
| 마 | 01a060dd-0004-744d-afe9-17a2fc0e724d | preview·meshy-5·1k tris·프롬프트=스크립트 `ma`(각진 바위 덩어리) | 1 | **5**(1006→1001) | **채택**(썸네일 게이트 통과 — 각진 로우폴리 덩어리) | Art/Generated/Ma/Boulder_Ma.fbx(비추적) · Prefabs/SpellFx_Ma.prefab |

### 마 Blender 반입 검수(§4.1 게이트 5항 실측)

① 트라이 983(≤1k ✓) ② 원치수 1.78×1.95×1.80(구형) ③ 피벗=중심 재계산 ④ 축=대칭 바위라 보상
불요(중심 피벗 대칭 케이스 — 소 결정과 같음) ⑤ 육안=단일 파츠·구멍 없음. 정규화 최장축 1.0
(0.91×1.0×0.92)·머티리얼 제거·FBX_SCALE_ALL 재익스포트. 조립: SpellFx_Ma=Fly07-01 배리언트,
Boulder는 **Projectile 자식**(착탄 시 비행부와 함께 꺼진다 — 잔존 바위는 「막」의 효과지 마가 아님),
스케일 0.5m·재질=M_SpellBodySpike(무광 틴트 호환). SpellVisualSet 마 FxPrefab 교체(ArcHeight 2.5 유지).

## 검증 (진행)

- 컴파일 클린(CS0170 1건 즉시 교정 — out var 확정 할당은 default 선행 초기화로)
- 게임 런타임 예외 0 확인 예정 · FlameJet 발화 컷=감사 씬(extras의 SpellFx_No 자동 발화)
- cone 실판정·탄속 체감·랜스·패링 가독의 최종 판정 = 예준 실플레이 게이트(§6-8)
