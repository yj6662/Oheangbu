# 리포 루트 공통 무시 목록.
# Unity 산출물(Library/Temp/obj 등)은 유니티 프로젝트 폴더의 Oheangbu/.gitignore가 담당한다.

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vs/
.idea/
*.user
*.DotSettings.user

# 로그·임시
*.log
*.tmp

# 비밀 — API 키는 절대 커밋하지 않는다(.env.example만 추적)
.env
.env.*
!.env.example

# 로컬 원본 백업(생성 에셋 데시메이트 전 원본) — 리포 밖 보관, 절차는 런 로그에 기입
Tools/MeshyRuns/_local/
