# KTP 통합04 — 금 사각 면과 소환수 재질 재검수

상태: **Main04 4종 × 5장 = 20장, 외부04 3종 × 5장 = 15장, 총35장 확인 완료.** 03의 금 계열 흰 판·사각 픽셀 문제는04의 실제 샘플에서 개선됐다. 03 실패 근거는 [03 보고서](C:/Users/yj666/Oheangbu/Art/SpellVFX120/KTP_INTEGRATED_REVIEW03.md)에 그대로 보존한다.

## 캡처 조건

`KTPIntegratedStills04`의 `016_ACF0`, `040_B188`, `073_C0AC`, `088_C19C` 각각 `_0`~`_4` 총20장을 모두 축소 재배치 시트로 열었고, 네 대상의 `_2`는 원본1280×720도 열람했다. 원본 그림을 바꾸지 않았다. Main Camera의 위치 (0,1.8,-3), 회전 (0,0,0), FOV60°는 03과 같다.

촬영 UTC는 **2026-09-09 06:17:03~06:17:15**다. JSON의 실제 DLL MVID는03과 같은 `78360a6f-5231-421c-8955-254a090925e4`다. 따라서 새 DLL이라는 가정으로 개선을 판단하지 않는다. 이번 비교는 촬영 시각과 폴더로 구분한 실제 자산/재질 결과의 비교다. 모두 예시cue이며 `gameplayConnectionVerified=false`다. 샘플은 각 Life의 7/22/42/64/90%다.

## 03 대비 결과

| 대상 | 03 문제 | 04 실제 관찰 | 판정 |
|---|---|---|---|
| 솜 088 | `_1`~`_4`에 밝은 직사각형 면이 발을 덮고 바닥 진 위에서 성벽/상자처럼 겹침. | 04의 같은 네 샘플에서 그 면은 없다. 흰 작은 점과 가는 선만 남고 격자 원진·호랑이 발·꼬리가 보인다. `_0`의 형성 구간에도 큰 판이 없다. | **해당 사각 면 결함 개선 확인**. 새 모델 자체나 소환 전체 완성 판정은 아니다. |
| 사 073 | `_2`~`_3`에서 밝은 흰 직사각형 알갱이가 디지털 픽셀처럼 두드러짐. | 같은 시각에 가는 백색 불꽃·작은 점이 나타난다. 기존의 크고 두꺼운 사각 알갱이는 사라졌다. `_3`의 가는 격자 원문양이 더 드러나고 `_4`에서는 옅은 잔상만 남는다. | **해당 사각 픽셀 결함 개선 확인**. 입자는 밝지만 블록이 화면 주제가 되는 상태는 아니다. |
| 곰 016 | 본체가 어둡고 캡슐이 목·앞몸을 가림. | 목·뒷몸의 회색 톤은 조금 더 보이지만 캡슐 가림과 뒷시점은 같다. 청록 입자·꽃진이 유지된다. | **Main만으로 재질 완료 판정 불가**. 몸통 가림과 수피/뿌리 디자인 미달을 별개로 유지한다. |
| 놈 040 | 본체 청흑색, 얼굴/몸통 가림. | 갈기의 붉은 부분이 조금 더 보이지만 Main의 검수 캡슐과 겹친 상태다. 붉은 불형과 꽃진은 유지된다. | **부분 개선 관찰**. 외부의 가림 없는 얼굴·몸통을 추가로 확인해야 한다. |

사슴·해태·호랑이는 모든 유지 샘플에서 같은 정적 포즈이며 마지막90%Life에서 몸의 투명도가 내려간다. 이 관찰을 리깅·보행·공격 애니메이션이나 완전 소멸의 통과로 기록하지 않는다.

![솜04: 큰 흰 판이 사라져 발과 바닥 격자가 보임](C:/Users/yj666/Oheangbu/Art/SpellVFX120/KTPIntegratedStills04/088_C19C_2.png)

![사04: 큰 사각 알갱이 대신 가는 불꽃과 점](C:/Users/yj666/Oheangbu/Art/SpellVFX120/KTPIntegratedStills04/073_C0AC_2.png)

## 외부04

`KTPExternalStills04`의 `016_ACF0`, `040_B188`, `088_C19C` 각각 `_0`~`_4` 총15장을 모두 축소 재배치 시트로 열었고, 세 대상의 `_2` 원본1280×720을 추가로 열었다. 외부03의 사슴·해태 `_2` 원본도 다시 열어 재질 차이를 비교했다. 원본 그림 안에 텍스트를 넣거나 원본을 변경하지 않았다.

촬영 UTC는 **2026-09-09 06:17:53~06:18:01**이다. 카메라 위치 (3.8,2.8,8), 회전 (18.0689,223.5312,0), FOV60°와 DLL MVID는 외부03과 같다. 카메라가 동굴 쪽을 바라보고 검수 대상 중심의 임시 캡슐은 없으므로, Main의 몸통 가림 없이 얼굴과 바닥 진을 함께 볼 수 있다. 왼쪽 뒤의 기존 검은 캡슐은 세 소환수를 가리지 않는다.

| 대상 | 외부04 실제 관찰 | 판정과 남은 제한 |
|---|---|---|
| 곰 016 | 뿔 끝·얼굴·긴 목·네 다리가 화면 안에 들어오며, 옅은 청록 입자와 긴 꽃잎 원문양이 본체 윤곽을 덮지 않는다.03보다 목과 뿔에 회색 디테일이 조금 더 보이지만 몸통은 계속 청흑색이다. | **프레이밍과 문양 가독성 수용**. 뿌리·수피로 만든 수호사슴보다는 어두운 일반 사슴으로 읽혀 목 속성 재질·설계 완성 판정은 보류한다. 발의 정확한 접지 오차는 측정하지 않았다. |
| 놈 040 | 넓은 얼굴·갈기·몸·꼬리와 다리 실루엣이 분리된다. 갈기의 주황 부분이 더 드러나고, 흰 곡선 원문양 주위의 주홍 불덩이가 금 소환진과 구별된다. 붉은 빛이 본체 전체를 덮는 과채도 상태는 아니다. | **불/금의 차별과 외곽 가독성 개선 확인**. 검은 몸통의 표면 재질은 여전히 읽기 어렵다. 한쪽 앞발과 뒷발이 들린 같은 포즈가 유지되어, 살아 있는 보행이나 착지보다 도약 중 멈춘 조형물의 인상이 남는다. 이 정적 샘플로 바닥 관통 수치를 단정하지 않는다. |
| 솜 088 | 중간 세 샘플의03 흰 사각 성벽/십자판이 없어졌다. 얼굴·앞발·뒷다리와 가는 격자 원진이 동시에 보인다. 주홍 불덩이가 없는 백색 선·점 구성으로 해태와 구별된다. 검은 등과 얼굴의 줄무늬 경계, 일부 금속색 모서리가 보인다. | **03의 발 가림 결함 개선 확인**. 본체는 여전히 청흑색이지만 큰 흰 판이 제거되어 명암 대비가 모델을 가리지는 않는다. 무늬·얼굴을 가까이 보는 재질 검수와 실제 움직임은 별도다. |

외부04에서 세 소환진과 모델이 화면 밖으로 잘리는 문제나 발을 덮는 불투명 상자 면은 관찰하지 못했다. 사슴의 청록·해태의 주홍·호랑이의 백색 문양 구성이 서로 구별되며,01의 거대한 공통 발광판 문제도 재발하지 않았다. 다만 중간 샘플은 본체 포즈와 원문양 형태가 거의 그대로 유지된다. 마지막90%Life에서 본체는 투명해지지만 원진은 남아 있으므로 **완전 소멸은 이 자료로 미검증**이다.

![솜 외부04: 사각 판이 제거된 실제 전신과 격자 원진](C:/Users/yj666/Oheangbu/Art/SpellVFX120/KTPExternalStills04/088_C19C_2.png)

![놈 외부04: 주홍 불형과 넓은 얼굴, 유지되는 정적 포즈](C:/Users/yj666/Oheangbu/Art/SpellVFX120/KTPExternalStills04/040_B188_2.png)

## 범위와 미완료 사항

이번04는 앞서 만든 Meshy 3종과 금 타격의 수정 확인이다. **반려된 장승064·수룡112는 여전히 미완료**이며, 이번04의 검수 대상에 없다. 두 실패 결과를 다른 형상으로 바꿔 Meshy 성공으로 표시하지 않는다. [두 재반려 원본](C:/Users/yj666/Oheangbu/Art/SpellVFX120/MeshySummons/Retry02/README.md).

방어막은 이번04에서 재촬영된 대상이 아니므로03의 독립 곡면 구성·내부 시점의 겹침 판정을 유지한다. 실제Play 수명·입력·전투·방어 기능 검사는 별도 담당의 결과이며 이 정지 이미지 보고서는 이를 대신하지 않는다. Unity/Blender, 앱 소스와 원본 이미지를 변경하지 않았다.
